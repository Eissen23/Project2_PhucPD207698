import React, {Component} from "react";
import {createRoot} from "react-dom/client";

export default class ResetAuthenticate extends Component{
    constructor(props){
        super(props);
    }

    render() {
      return (
        <div>
          <h1>Password reset authentication</h1>
        </div>
      )
    }
}
