import React, {Component} from "react";
import {createRoot} from "react-dom/client";

export default class Activate extends Component{
    constructor(props){
        super(props);
    }

    render() {
      return (
        <div>
          <h1>Activate authentication</h1>
        </div>
      )
    }
}
